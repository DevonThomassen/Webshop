{
    "name": "webshop",
    "devDependencies": {
        "http-server": "^0.11.1",
        "microbundle": "^0.9.0"
    },
    "scripts": {
        "start": "http-server page --silent & $npm_execpath run watch",
        "watch": "microbundle watch --format umd --entry page/src/index.js --output page/dist/bundle.js"
    },
    "files": [
        "dist"
    ],
    "dependencies": {
        "textsplitter": "^1.0.5"
    }
}
